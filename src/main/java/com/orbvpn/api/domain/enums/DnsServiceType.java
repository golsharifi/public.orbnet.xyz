package com.orbvpn.api.domain.enums;

public enum DnsServiceType {
    STREAMING,  // International streaming services (Netflix, Hulu, etc.)
    REGIONAL     // Regional services for diaspora (banks, government, etc.)
}
